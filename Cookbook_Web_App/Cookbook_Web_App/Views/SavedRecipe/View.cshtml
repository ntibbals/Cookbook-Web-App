@model IEnumerable<Cookbook_Web_App.Models.Comments>
@{
    ViewData["Title"] = "View";
    Layout = "_Layout";
}
<h1>Comments</h1>
<div>

    <div>

        <div>
            @foreach (var item in Model)
            {

                <div>
                    @Html.DisplayFor(modelItem => item.Comment)
                </div>

            }
        </div>
    </div>
</div>

<a asp-action="Index">Back to List</a>